#include<stdio.h>
int main()
{
    int x,i,sum=0;
    printf("Enter the integer: ");
    scanf("%d",&x);
    for (i=1;i<=x;i++)
    {
        if (x%i==0)
        {
            sum=sum+i;
        }
    }
    printf("Sum of divisors = %d",sum);
    return 0;
}
